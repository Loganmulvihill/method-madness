//All methods are tested live at https://repl.it/ using JavaScript.

//Simple Object.assign() method call
const first = { a: 1, b: 2 };
const second = { a: 2, c: 3 };

const three = Object.assign(first, second);

console.log(first);

console.log(three);
